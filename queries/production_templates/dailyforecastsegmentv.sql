DECLARE project_id STRING DEFAULT 'your-production-project';
DECLARE dataset_name STRING DEFAULT 'prod_hotel_analytics';
DECLARE table_name STRING DEFAULT 'dailyforecastsegmentv';

EXECUTE IMMEDIATE FORMAT("""
  CREATE OR REPLACE TABLE `%s.%s.%s` (
    select property OPTIONS(description='Standardized select.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    when account OPTIONS(description='Standardized when.'),
    else NULL OPTIONS(description='Standardized else.'),
    end AS OPTIONS(description='Standardized end.'),
    where account OPTIONS(description='Standardized where.'),
    from MappedData OPTIONS(description='Standardized from.'),
    select property OPTIONS(description='Standardized select.'),
    case WHEN OPTIONS(description='Standardized case.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    end AS OPTIONS(description='Standardized end.'),
    value AS OPTIONS(description='Standardized value.'),
    from FilteredData OPTIONS(description='Standardized from.'),
    unpivot ( OPTIONS(description='Standardized unpivot.'),
    value FOR OPTIONS(description='Standardized value.'),
    case WHEN OPTIONS(description='Standardized case.'),
    when month OPTIONS(description='Standardized when.'),
    when month OPTIONS(description='Standardized when.'),
    end AS OPTIONS(description='Standardized end.'),
    from Unpivoted OPTIONS(description='Standardized from.'),
    select property OPTIONS(description='Standardized select.'),
    case WHEN OPTIONS(description='Standardized case.'),
    else month OPTIONS(description='Standardized else.'),
    end AS OPTIONS(description='Standardized end.'),
    from DaysInMonth OPTIONS(description='Standardized from.'),
    select property OPTIONS(description='Standardized select.'),
    from DailyBreakdown OPTIONS(description='Standardized from.'),
    where segment OPTIONS(description='Standardized where.'),
    group BY OPTIONS(description='Standardized group.'),
    select db OPTIONS(description='Standardized select.'),
    case WHEN OPTIONS(description='Standardized case.'),
    and db OPTIONS(description='Standardized and.'),
    and db OPTIONS(description='Standardized and.'),
    and db OPTIONS(description='Standardized and.'),
    then SAFE OPTIONS(description='Standardized then.'),
    else db OPTIONS(description='Standardized else.'),
    end AS OPTIONS(description='Standardized end.'),
    from DailyBreakdown OPTIONS(description='Standardized from.'),
    left JOIN OPTIONS(description='Standardized left.'),
    on db OPTIONS(description='Standardized on.'),
    and db OPTIONS(description='Standardized and.'),
    and db OPTIONS(description='Standardized and.'),
    and db OPTIONS(description='Standardized and.'),
    from FinalData OPTIONS(description='Standardized from.')
  )
""", project_id, dataset_name, table_name);